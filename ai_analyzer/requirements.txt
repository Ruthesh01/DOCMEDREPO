# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9     # for file uploads

# ── AWS S3 ────────────────────────────────────────────────────────────────────
boto3==1.34.69

# ── OpenAI (primary model) ────────────────────────────────────────────────────
openai==1.25.0
httpx==0.27.0               # async HTTP client used by openai SDK

# ── OCR & PDF ─────────────────────────────────────────────────────────────────
pdfplumber==0.11.0          # digital PDF text extraction
pdf2image==1.17.0           # scanned PDF → image conversion (requires poppler)
pytesseract==0.3.10         # OCR (requires tesseract-ocr system package)
Pillow==10.3.0              # image preprocessing

# ── HuggingFace fallback model ────────────────────────────────────────────────
transformers==4.40.1
torch==2.3.0                # CPU inference — change to torch[cuda] for GPU

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.7.1
